[package]
name = "agent_staking"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rubixwasm-std = { path = "../../../Rubix/rubix-wasm/packages/std" }
